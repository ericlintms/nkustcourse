擴展前一個單元的 `SmaCrossStrategy` 策略。

1.  在 `init` 方法中，計算兩條簡單移動平均線 (SMA)：一條快線 (10週期) 和一條慢線 (20週期)。
2.  在 `next` 方法中，實作一個均線黃金交叉策略：
    *   當快線由下往上穿越慢線時 (黃金交叉)，買入。
    *   當快線由上往下穿越慢線時 (死亡交叉)，賣出。

(提示: 可以使用 `backtesting.lib.crossover` 函式來判斷穿越)
