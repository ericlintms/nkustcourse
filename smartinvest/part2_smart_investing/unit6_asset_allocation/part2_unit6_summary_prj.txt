撰寫一個名為 `calculate_portfolio_return` 的函式。

此函式接受兩個列表作為參數：
1.  `weights`: 一個包含投資組合中每項資產權重的列表。
2.  `returns`: 一個包含這些資產預期回報率的列表。

函式應計算並回傳投資組合的總預期回報率。

**要求**:
-   函式需要檢查 `weights` 和 `returns` 的長度是否相等。
-   函式需要驗證 `weights` 的總和是否為 1.0。如果不符合，應回傳錯誤訊息。
